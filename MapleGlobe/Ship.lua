

function Ship.cacheScriptFunc(self)

end

function Ship.contimoveEliEre(self, player, udc)

end

function Ship.contimoveEreEli(self, player, udc)

end

function Ship.contimoveEreOrb(self, player, udc)

end

function Ship.contimoveOrbEre(self, player, udc)

end

function Ship.contimoveRieRit(self, player, udc)

end

function Ship.contimoveRitRie(self, player, udc)

end

function Ship.contimoveRitSDI(self, player, udc)

end

function Ship.contimoveSDIRit(self, player, udc)

end

function Ship.elevator(self, p)

end

function Ship.get_ticket(self, player, udc)

end

function Ship.goOutWaitingRoom(self, player, udc)

end

function Ship.move_EliEre(self, player, udc)

end

function Ship.move_elin(self, player, udc)

end

function Ship.move_EreEli(self, player, udc)

end

function Ship.move_EreOrb(self, player, udc)

end

function Ship.move_OrbEre(self, player, udc)

end

function Ship.move_RieRit(self, player, udc)

end

function Ship.move_RitRie(self, player, udc)

end

function Ship.move_RitSDI(self, player, udc)

end

function Ship.move_SDIRit(self, player, udc)

end

function Ship.sell_ticket(self, player, udc)

end
